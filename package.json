{
    "name": "management",
    "version": "0.1.0",
    "scripts": {
        "client": "cd client && yarn start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"yarn serer\" \"yarn client\""
    },
    "dependencies": {
        "body-parser": "^1.20.1",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "concurrently": "4.0.0"
    }
}
